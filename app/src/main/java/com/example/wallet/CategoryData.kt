package com.example.wallet

import com.example.wallet.model.Categories

object CategoryData {
    val categoryList = listOf(
        Categories(
            imageResId = R.drawable.makanan,
            uang = "-$18.35",
            waktu = "15:00 - April 24",
            kegiatan = "Pizza Delivery"
        )
    )
}